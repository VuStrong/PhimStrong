@{
    Layout = "/Views/Shared/_Layout.cshtml";
}

<section class="user-info py-4 row justify-content-center align-items-center">
    <div class="col col-lg-1 col-md-1 mb-lg-0">
        <partial name="_ManageNav"></partial>
    </div>
    <div class="col col-lg-6 col-md-10 col-12 ms-md-5">
        <div class="card" style="border-radius: .5rem;">
            @RenderBody()
        </div>
    </div>
</section>

@section Scripts {
    @await RenderSectionAsync("Scripts", required: false)
}

